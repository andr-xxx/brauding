const QueueModel = require('../models/queue');

module.exports = async (newTasks) => {
  if (!taskList) return;

  taskList.forEach(item => {

  })

  // const queueList = QueueModel.getQueueListFromDateNow
};